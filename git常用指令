…or create a new repository on the command line

echo "# 11" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/Amaninthedark/11.git
git push -u origin master

git clone

克隆下来后直接 
git pull
就可以同步了 